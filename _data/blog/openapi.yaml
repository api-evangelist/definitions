Swagger: "2.0"
info:
  title: API Evangelist Definitions Blog
  description: An API for accessing the blog posts included in this research.
  version: 1.0.0
host: definitions.apievangelist.com
basePath: /apis
schemes:
  - http
produces:
  - application/json
consumes:
  - application/json
paths:
  /products/:
    get:
      summary: Get Blog Posts
      description: Returns all the blog posts in this research.
      responses:
        200:
          description: Successful Response
          schema:
            type: array
            items:
              $ref: '#/definitions/blog'
      tags:
        - Blog
        - API
definitions:
  product:
    properties:
      view_sort:
        description: A letter to sort in the UI.
        type: string
      name:
        description: The name of the product.
        type: string
      identifier:
        description: A unique identiefer for the product.
        type: string
      url:
        description: A URL to find more information about product.
        type: string
      description:
        description: A descripton of the product.
        type: string
      image:
        description: An image representation for a product.
        type: string
      imagewidth:
        description: An image width for the product.
        type: integer
      category:
        description: A category for the product.
        type: string
      brand:
        description: A brand associated with the product.
        type: string
